---
name: sdlc-NNN-name
description: 显式调用时完成什么，以及何时使用。
disable-model-invocation: true
---

# SDLC NNN · 名称（缩写）

## 适用范围

写清输入、产物、非目标和一个最小调用示例。

## 约定与边界

说明准确 Authority、独占执行、决策/写入策略、只读与效果授权边界。

## 子命令

| 子命令 | 职责 | 可能写入 |
|---|---|---|

逐行匹配实际 interface.json，不把模板占位当作已实现命令。

## 参数

| 参数 | 短参数 | 语义／默认值 |
|---|---|---|

列出实际共享参数与阶段扩展；写明默认值、可重复性和禁用条件。

## 执行流程

1. 解析命令。
2. 准确读回与预检。
3. 按合约执行、验证和停止。

## 输出与完成条件

分别说明 summary/json/debug，不能以写入成功替代产品/Artifact Gate。

## 资源索引

链接实际存在的 bundled contract/interface/runtime；不引用开发过程产物。
