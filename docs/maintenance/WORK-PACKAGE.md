# 单分支整理与全流程验证准备

## 基线与授权

- 基线：`9d9dbf8bc1b9241f80af53cdf5b0426fcbfe3ab9`，Tree `d90e81a94e5b7b5628eb6849cef15bb3f96deb36`。
- 唯一写分支：`refactor/skill-unification-cleanup`；最终仅一次合入 main，当前不合并。
- 用户于 2026-09-06 授权：修复已知评测问题、精简重复/过期开发测试与过程留痕、统一八个 Skill 的格式，并准备一次最终全流程验证。
- 原生 Client 独立认证暂不作为本轮门禁。用户报告已手动验证但未留痕；不伪造认证记录、不重复启动八个宿主会话。

## 顺序

1. 冻结基线、保留可恢复 Git bundle，测量文件/测试规模。
2. 修复 IMP 固定评测引用与 REQ 测试导入/重复收集；保留行为 Expected。
3. 将一次性 Evidence、原生 trace、旧 Goal、失败恢复和逐轮报告移出当前工作树；以准确历史 Git 对象和备份索引保留恢复能力，不重写历史。
4. 将测试分为日常快速检查、一次去重的完整回归、严格/外部全链检查；删除有明确替代的临时脚手架或重复案例，不删除授权、只读、篡改、失败恢复等有效覆盖。
5. 统一 SKILL.md 的元数据、适用边界、约定、子命令表、参数表、执行流程、输出与失败/完成条件；以实际 interface.json、共享参数和 Runtime 为准。制定模板和自动风格/参数一致性检查。
6. Web 执行可运行的检查和回归。需要真实 OS 沙箱/全流程产品上下文的部分，交同一分支给 Client 一次执行；不因原生留痕缺失阻塞。

## 保留与清理边界

保留 `docs/v1.x/**`、有效领域契约、共享 Runtime、安全门禁、正式 Case ID/Expected 和必要 Fixture。可清理开发归档及暂存流程工具，但须检查调用依赖并提供迁移/删除依据。生产效果、依赖安装、系统配置修改、main 写入、历史重写和 `.github/**` 变更不在本工作包内。

旧测试结果仅证明其原始源码，不能用于宣称整理后 PASS。归档清理不是秘密擦除：历史仍可恢复。未来执行日志输出到忽略目录或仓库外，不再逐轮回填大批历史产物。

## 完成条件

固定案例均可解析；测试收集无重复且保留有效覆盖；八份 Skill 结构一致且参数/命令与机器定义相符；全部声明为通过的命令均有实际回执；新分支正常 fast-forward 检查点与 Draft PR 可恢复；Client 交接只包含剩余实际验证，不自动合并。
