# DSN

读取准确REQ与现有代码，按适用领域保存设计、决策、理由、替代方案、detail和结构化覆盖。
定义验收方法与收敛审阅；test需要command执行，Agent审阅使用inspection/analysis等类型。
设计完成要求需求/验收覆盖，不创建固定空域；提交后创建PLN草稿。

验收条目可由required acceptance Check或合法原生release_readback Check覆盖。
后者固定command与argv=["@runtime","delivery.readback"]，静态覆盖不代表已通过：
VFY不提前执行回读，RLS仍须真实包与适用回读结果。完整workspace.export在RLS之后另行验证，不能由包回读推导。
