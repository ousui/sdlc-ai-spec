interface:
  display_name: "SDLC GitHub · 仓库协作"
  short_description: "显式读取准确 GitHub 对象、核对当前身份并以防重放回执执行基础协作写入"
  default_prompt: "使用 $sdlc-github 和其已安装的 scripts/run 处理明确授权的 GitHub 对象。"
policy:
  allow_implicit_invocation: false
