# 实施交接：先完成固定基线与最小纵向链路

当前：仅规划提交。没有可安装的 v0 实现；旧根目录插件不是本次交付物。

先读 [PLAN.md](PLAN.md) 与 [SOURCE-REVIEW.md](SOURCE-REVIEW.md)。用户明确允许实施后，创建独立实施分支，例如 impl/spec-kit-plugin-v0。不要自动合并当前规划分支到 main。

## 已确认方向

用户级插件提供共享资源；每个业务项目以 sdlc-init 初始化 .sdlc；按固定 Spec Kit 核心流程工作。v0 不翻译、不新增团队规则、不加入 GitHub，不依赖旧 SDLC v1/v2。依据用户本次明确要求，旧版阶段编号、ArtifactStore、Runtime 或独占 Skill 工作流不得重新作为 v0 产品要求。

## 第一技术检查点

P0→P1：固定 github/spec-kit@a4e25ce6b96dc8e85f84206c6a54353fa9c5260b，分别采集 codex/claude/cursor-agent 的上游 sh/无事件/无扩展/无预设生成物，然后先在 Codex 原生用户级插件上打通 init→specify→plan。

这一检查点的输出限于 v0/ 新子树与本计划的状态更新：固定来源记录、实际生成清单、必要共享资源与路径补丁、最小可安装包、对应测试和操作说明。根目录旧代码和三个旧 Manifest 不改；新包不能读取它们。

在最小链路工作之前，不提前实现全部剩余 Skill，不加入 GitHub、翻译、Windows、自动编排或 Runtime 重写。不通过复制脚本到每个业务项目绕过全局资源目标。

## 验证与继续方式

用同一个只读插件包在 A/B 两个独立项目生成各自 spec.md 与 plan.md；验证项目定位、无 CLI 回调、重复 init、不覆盖人工文件、无跨项目状态、原生发现与参数/脚本调用。

能运行普通脚本但没有实际客户端时，只报告脚本验证，不能登记原生通过。遇到宿主不能取得插件路径或不能读取/执行用户级资源，明确记录该 surface 的阻塞；不放宽权限或用人工读取 Skill 冒充插件加载。

P0-P4 是技术检查点，不是五次强制人工审批。后续用户若只授权 P0→P1，则完成后停止；若授权整个 v0，则最小链路通过后可在同一授权范围内连续完成 P2-P4，不反复索要阶段批准。当前规划请求本身不授权运行实施。

交付时说明准确提交、实际测试、剩余缺口，不将计划中的验收写成已有通过结果。
