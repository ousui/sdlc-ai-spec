# v1.1 Spec Delta Plan

## 1. 工作包状态与边界

本文件只定义从 `docs/v1.0/` 到 `docs/v1.1/` 的 Spec 变更计划。它不是正式 Spec，不进入任何 Artifact 的 `Evaluation Contract Set`，也不授权创建 `docs/v1.1/`、SQLite 数据库、Schema、Store 模块、Projection 或 Skill。

当前状态：SQLite-only scope correction 已完成，等待独立、只读审查。审查通过并获得新的创建授权前不得创建 `docs/v1.1/`。

本工作包输入基线：

- 权威仓库：`blade-cdn/sdlc-ai-spec`；
- 分支：`main`；
- HEAD：`e23b982aa80d545f88c6fb0dfb3cd0e5229190bf`；
- 工作包开始时 `git status --short` 为空；
- Origin Fetch / Push 均指向 `git@github.com:blade-cdn/sdlc-ai-spec.git`，有效 SSH Host 最终为 `github.com`。

权威输入：

- `docs/architecture/artifact-store-and-projection.md`：Local SQLite Store、Human Review View 与 Runtime Workspace 架构决策；
- `docs/v1.0/core-spec.md` 与当前各 Phase / Domain Spec：既有领域语义；
- 当前工作包给出的固定决定。

## 2. v1.0 / v1.1 版本边界

### 2.1 v1.0 保持不变

- `docs/v1.0/` 保持字节不可变，并继续由现有 `SHA256SUMS` 验证；
- v1.1 是新的完整 Spec Snapshot，不是对 v1.0 的原地修改；
- v1.0 Frozen Artifact 始终按其准确 `Evaluation Contract Set` 解释；
- 本计划不设计 v1.0 自动迁移工具。

### 2.2 v1.1 唯一语义变化

v1.1 解除 `artifacts/**`、`revision-index.md`、`revisions/<NNNNNN>/`、主文件路径和目录扫描对 Canonical Artifact Authority 的绑定，改由逻辑 Artifact Store Contract 表达：

- Artifact 与 Revision 分配；
- Canonical Blob 读写；
- Revision 状态转换；
- 准确 Reference 解析；
- digest 验证；
- 失败关闭。

Core、CTX、DSN、IMP 只移除上述物理目录耦合，不改变其业务语义。其他 Phase 和 Domain Spec 只进行必要的 Snapshot 版本与引用更新。

### 2.3 必须保持的领域 Contract

以下内容保持不变：

- `sdlc-ai-spec/artifact/v1`；
- `sdlc-ai-spec/project-context/v1`；
- `sdlc-ai-spec/final-confirmation-authority/v1`；
- Artifact ID、CTX ID、Revision、Base Revision 与项目范围唯一性；
- `open / frozen / abandoned` Revision State；
- `draft / waiting_input / failed / ready / ready_with_exception` Artifact Status；
- Context、Artifact、Item、Member、Spec、Authority 与 VCS Reference 的现有字段、用途和语法；
- Evidence、Exception、Open Item、Disposition、Check、Gate、Final Confirmation 与责任边界；
- Frozen Revision 不可修改；
- 不使用 `latest/current` fallback；
- 完整 Canonical Markdown/YAML Blob 与 Canonical Blob SHA-256。

发现必须改变任一上述内容时停止 v1.1 编制，不得以存储修正为由静默改变领域 Contract。

## 3. 唯一新增正式 Spec

v1.1 只新增一份正式 Spec：

```text
artifact-store-spec.md
```

该 Spec 位于 Core 之后、Project Context 或当前 Phase Spec 之前，进入全部 v1.1 Canonical Artifact 的 `Evaluation Contract Set`。它只定义以下最小逻辑操作：

- `initialize`；
- `allocate artifact`；
- `allocate revision`；
- `read revision`；
- `write open revision`；
- `freeze revision`；
- `abandon revision`；
- `resolve exact reference`；
- `verify digest`。

它还必须定义：

- 一个项目只有一个 Canonical Store；
- 同一 Artifact 同时最多一个 `open` Revision；
- Revision 单调增加且不复用；
- Frozen Revision 不可修改；
- 写操作具有原子事务边界；
- 准确 Reference 只解析指定 Revision；
- Store 不可用、损坏或事务失败时 fail closed；
- 不静默降级到文件或临时目录。

该 Spec 不定义 SQLite 表、列、索引、PRAGMA、WAL、Migration、固定数据库路径、配置文件或实现代码。当前 Plugin 使用 Local SQLite 的决定只登记在 `docs/plugin-development/DEVELOPMENT.md` 和架构文档中。

Human Review View 保持为 Plugin Projection。其文件格式、映射 Schema 和编辑器适配留到 Skill 实现阶段，不新增独立 Projection Spec。

## 4. 逐文件影响矩阵

Change Class 只使用 `normative-change`、`reference-update-only`、`new-v1.1-spec` 与 `non-contract-document`。`reference-update-only` 只允许更新 Snapshot `version`、路径、链接或摘要引用，不得改变业务规则。

| File | Change Class | Required v1.1 Change | Semantic Guardrail |
|---|---|---|---|
| `core-spec.md` | normative-change | 移除固定 Artifact 根目录、Lifecycle 目录表、主文件名、Revision Index 文件、六位 Revision 目录与目录驱动 Resolve；接入逻辑 Artifact Store Contract | 不改变 Artifact 字段、ID、Revision、Reference、Status、Gate、Final Confirmation 与 digest 语义 |
| `000-ctx-spec.md` | normative-change | 移除 `000-ctx` 固定目录、Revision Index 文件和 Revision 目录；使用 Store 中唯一 CTX Lineage 与准确 Revision | 不改变一 Project Boundary 一 CTX Lineage、CTX 字段、Basis、Status 与 Gate |
| `100-req-spec.md` | reference-update-only | 仅更新 Snapshot 版本与必要引用 | REQ 字段、行为与 Gate 不变 |
| `200-dsn-spec.md` | normative-change | 移除 DSN Artifact Set 的固定目录树；以 Canonical Blob 和稳定 Member 表达同一 Artifact Set | 不把 Domain Member 升级为独立 Artifact，不改变 `DOM-*`、`SUP-*` 或父 Gate |
| `200-dsn-domains/110-workflow-state.md` | reference-update-only | 仅更新 Snapshot 版本与必要引用 | Workflow、State、Transition 与 Gate 不变 |
| `200-dsn-domains/120-ux-interaction.md` | reference-update-only | 仅更新 Snapshot 版本与必要引用 | Interaction Contract 不变 |
| `200-dsn-domains/130-ui-content.md` | reference-update-only | 仅更新 Snapshot 版本与必要引用 | UI / Content Contract 不变 |
| `200-dsn-domains/140-accessibility-i18n.md` | reference-update-only | 仅更新 Snapshot 版本与必要引用 | Accessibility、i18n 与聚合 Gate 不变 |
| `200-dsn-domains/210-system-architecture.md` | reference-update-only | 仅更新 Snapshot 版本与必要引用 | Architecture Domain Contract 不变 |
| `200-dsn-domains/220-components-modules.md` | reference-update-only | 仅更新 Snapshot 版本与必要引用 | Component、Module 与 Ownership Contract 不变 |
| `200-dsn-domains/230-interfaces-integration.md` | reference-update-only | 仅更新 Snapshot 版本与必要引用 | Interface / Integration Contract 不变 |
| `200-dsn-domains/240-data-design.md` | reference-update-only | 仅更新 Snapshot 版本与必要引用 | 产品 Data Contract 不变 |
| `200-dsn-domains/310-security-privacy-compliance.md` | reference-update-only | 仅更新 Snapshot 版本与必要引用 | Security / Privacy / Compliance Contract 不变 |
| `200-dsn-domains/320-performance-capacity.md` | reference-update-only | 仅更新 Snapshot 版本与必要引用 | Performance / Capacity Contract 不变 |
| `200-dsn-domains/330-reliability-recovery.md` | reference-update-only | 仅更新 Snapshot 版本与必要引用 | Reliability / Recovery Contract 不变 |
| `200-dsn-domains/340-compatibility-migration.md` | reference-update-only | 仅更新 Snapshot 版本与必要引用 | 产品 Compatibility / Migration Contract 不变 |
| `200-dsn-domains/350-maintainability-extensibility.md` | reference-update-only | 仅更新 Snapshot 版本与必要引用 | Maintainability / Extensibility Contract 不变 |
| `200-dsn-domains/410-deployment-configuration.md` | reference-update-only | 仅更新 Snapshot 版本与必要引用 | 产品 Deployment / Configuration Contract 不变 |
| `200-dsn-domains/420-observability-operability.md` | reference-update-only | 仅更新 Snapshot 版本与必要引用 | Observability / Operability Contract 不变 |
| `200-dsn-domains/510-verifiability-vfy-strategy.md` | reference-update-only | 仅更新 Snapshot 版本与必要引用 | VFY Strategy Contract 不变 |
| `300-pln-spec.md` | reference-update-only | 仅更新 Snapshot 版本与必要引用 | Work Item、Scope Token、Dependency 与 Gate 不变 |
| `400-imp-spec.md` | normative-change | 用 Artifact / Revision 分配、读取与放弃操作替换 Claim 后创建或恢复目录、索引文件和主文件骨架的步骤 | Claim Authority、Claim State、Attempt、Owner、Dependency、Rework 与失败关闭顺序不变 |
| `500-vfy-spec.md` | reference-update-only | 仅更新 Snapshot 版本与必要引用 | Target、Method、Conclusion、Return 与 Gate 不变 |
| `600-rls-spec.md` | reference-update-only | 仅更新 Snapshot 版本与必要引用 | Release、Conclusion、Follow-up 与 Gate 不变 |
| `artifact-store-spec.md` | new-v1.1-spec | 定义最小逻辑 Artifact Store Contract | 不定义 SQLite Schema，不重定义 Core 领域字段 |
| `README.md` | non-contract-document | 更新 v1.1 索引、阅读顺序和 25 份正式 Spec 数量 | 不新增规范规则 |
| `overview.md` | non-contract-document | 更新 Store 与 Human Review View 的导航说明 | 不新增规范规则 |
| `ai-human-collaboration.md` | non-contract-document | 字节复制 | 不因存储变化改写人工 / AI 责任 |
| `SHA256SUMS` | non-contract-document | 最后生成并覆盖 25 份正式 Spec | 不包含索引、概览、协作建议或摘要文件自身 |

v1.1 正式 Spec 数量为 25：v1.0 的 24 份正式 Spec 加上 `artifact-store-spec.md`。连同 `README.md`、`overview.md`、`ai-human-collaboration.md` 和 `SHA256SUMS`，`docs/v1.1/` 预期总文件数为 29。

## 5. Core、CTX、DSN 与 IMP 的精确边界

### 5.1 Core

- 用逻辑 Artifact、Revision、Canonical Blob 与 Store 操作替换固定目录布局；
- 将准确 Resolve 改为 `resolve exact reference`；
- 将分配、写入、Freeze 与 Abandon 改为事务语义；
- 在 v1.1 `Evaluation Contract Set` 中加入准确 Artifact Store Spec 引用；
- 保持全部领域字段、Reference、Gate、Final Confirmation 和摘要规则不变。

### 5.2 CTX

- 用 Store 中唯一 CTX Lineage 替换 `artifacts/000-ctx/` 物理布局；
- 保持 Context Reference 为准确 `<CTX-ID>@<Revision>`；
- 保持 Project Boundary、Basis、Item、Status、Gate 与刷新规则不变。

### 5.3 DSN

- 用同一 Revision 的 Canonical Blob 与稳定 Member 表达 DSN Artifact Set；
- 保持 16 份 Domain Spec、固定 Domain Member Name、`DOM-*`、`SUP-*` 和父 Gate；
- 不把每个 Design Decision 或 Domain 内容规范化为关系表。

### 5.4 IMP

- Claim 成功后通过 `allocate artifact`、`allocate revision`、`write open revision` 和读回验证物化 IMP Artifact；
- 物化或校验失败时只使用 `abandon revision` 终结已预留 Revision；
- 完整分配、写入和读回前不得修改产品；
- Claim Authority 与 Artifact Store Authority 的既有先后和失败关闭边界不变。

## 6. 后续 v1.1 编制顺序

只有 SQLite-only Delta Plan 的独立只读审查通过并获得新的创建授权后，才按以下顺序执行：

1. 重新验证 `docs/v1.0/SHA256SUMS`，冻结 24 份正式 Spec 清单和本矩阵；
2. 创建 v1.1 目录骨架，复制 24 份正式 Spec 与 3 份非 Contract 阅读文档；
3. 新建 `artifact-store-spec.md`，只写最小逻辑 Store Contract；
4. 修改 `core-spec.md`、`000-ctx-spec.md`、`200-dsn-spec.md`、`400-imp-spec.md`，只解除物理目录耦合；
5. 更新其他 Phase 和全部 16 份 Domain Spec 的 `version: "1.1"` 与必要 Snapshot 引用；
6. 更新 `README.md` 与 `overview.md`，确认协作建议正文未变化；
7. 执行全局字段、Reference、Contract ID、Gate、固定路径残留和非预期语义 Diff 检查；
8. 最后生成覆盖 25 份正式 Spec 的 `SHA256SUMS`；
9. 启动独立、只读 Spec Review；Review 通过前不实现 Store、Projection 或 Skill。

步骤 3 至 5 不得以并行修改掩盖跨 Spec 冲突；任一领域语义需要变化时停止。

## 7. 静态验证计划

- v1.0：28 个文件，其中 24 份正式 Spec；现有 `SHA256SUMS` 前后均通过；
- v1.1：预期 29 个文件，其中 25 份正式 Spec；
- v1.0 不出现任何 Git Diff；
- v1.1 不出现矩阵之外的文件、配置、Schema、Script 或实现；
- Core / CTX / DSN / IMP 不再通过 `artifacts/`、`revision-index.md`、`revisions/`、六位目录或主文件路径执行 Authority、分配、Resolve、Freeze 或 Abandon；
- 其他 Phase 与 Domain Spec 除版本和必要引用外正文语义不变；
- Context / Artifact / Item / Member / Spec / Authority / VCS Reference 语法逐字保持；
- 三个现有 Contract ID 保持 `/v1`；
- Frozen Revision、Status、Gate、Final Confirmation 与准确 Reference 语义保持；
- `latest/current` 继续只作为禁止 fallback 的反例；
- Human Review View 不进入 Canonical Artifact Authority 或 `Evaluation Contract Set`；
- 不创建自动迁移工具，不定义多存储切换、远程能力、跨存储寻址、分布式协调或保留策略矩阵。

## 8. Review Gate 与唯一下一工作包

本计划完成不等于 v1.1 Source of Truth 已完成，也不提供运行时实现证据。

下一唯一工作包是：对收敛后的 SQLite-only v1.1 Delta Plan 进行独立、只读审查。

审查通过前不得创建 `docs/v1.1/**`，不得批准或实现 `sdlc-project-context`，不得实现 SQLite、ArtifactStore 模块、Projection、迁移工具或任何 Skill。
