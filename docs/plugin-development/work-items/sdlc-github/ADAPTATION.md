# 三端配置与可选使用反馈

Codex、Cursor、Claude Code 共用一个 Service 和生产 stdio Server，仍各有薄配置。安装器生成相同模式的准确解释器 launcher；程序测试校验三端配置、剥离开发资源后的执行与稳定 data 复用。
按照合入的 main@7a454c76 决定，原生独立认证不再作为门禁。原 Codex/其它客户端结果仅属于历史源码，不继承到本次修复；真实冷启动可由用户本地反馈。
任何原生 no-scan 结论须有明确操作归属/完整访问证据，不能只以 10,000 文件摘要相同推导。宿主必要发现与 Skill 委派的无关扫描必须分开。缺客户端或模型服务故障不伪造证书，不阻塞其他可执行验证。
